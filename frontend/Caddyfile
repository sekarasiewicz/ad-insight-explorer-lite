# API server
:{$PORT} {
	handle /api/* {
		reverse_proxy {$BACKEND_URL}
	}

	handle /health {
		reverse_proxy {$BACKEND_URL}
	}

	handle /docs {
		reverse_proxy {$BACKEND_URL}
	}

	# Fallback to React app
	handle /* {
		root * /usr/share/caddy/html
		file_server
		try_files {path} /index.html
	}
} 